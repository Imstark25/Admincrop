import 'package:flutter/material.dart';

class AppInfoView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('App Info', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),
    );
  }
}