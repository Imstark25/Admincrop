// lib/views/crop_management_view.dart
import 'package:flutter/material.dart';
class CropManagementView extends StatelessWidget {
  const CropManagementView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Crop Management Page', style: TextStyle(fontSize: 24)));
  }
}