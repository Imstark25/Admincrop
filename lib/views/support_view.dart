import 'package:flutter/material.dart';

class SupportView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Support Page', style: TextStyle(fontSize: 24)));
  }
}